# A generic, single database configuration.

[alembic]
script_location = alembic
prepend_sys_path = .
version_path_separator = os

# Fallback URL; overridden dynamically from config.py in env.py
sqlalchemy.url = sqlite:///data/run_intelligence.db